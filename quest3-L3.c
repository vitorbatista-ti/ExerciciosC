#include <stdlib.h>
#include <stdio.h>
int main ( ) {

    char str [50];
    char str2 [50];
    printf ("digite o seu nome: ");
    gets (str);
    printf ("digite o nome da sua namorada : ");
    gets (str2);
    sprintf( resultado, "%s:%s", str1, str2);
    return 0;
}
